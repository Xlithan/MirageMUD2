namespace Bindings
{
    // Shared direction enum used by client & server packets
    public enum Direction
    {
        North = 0,
        East = 1,
        South = 2,
        West = 3,
        Up = 4,
        Down = 5
    }
}